spring:
  application:
    name: novel-service
  datasource:
    url: jdbc:postgresql://localhost:5432/novelDB
    username: postgres
    password: 365891
  jpa:
    hibernate:
      ddl-auto: create
    defer-datasource-initialization: true
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      mode: always
server:
  port: 8083
#jwt:
#  secretKey: wilhelm.theresia.nasa.tsukasa.ayanokoji.subaru
novelforge:
  security:
    jwt:
      secret-key: wilhelm.theresia.nasa.tsukasa.ayanokoji.subaru
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

  instance:
    hostname: localhost
    prefer-ip-address: false
    instance-id: ${spring.application.name}:${server.port}